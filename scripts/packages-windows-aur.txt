# SevenOS Windows Bridge optional AUR helpers
#
# These are intentionally optional. SevenOS can still create a Windows VM from
# a user-provided official ISO without them.
quickemu

# Optional native-like app surfaces. Bottles Flatpak remains the preferred
# default, but these make the Windows layer feel complete when AUR is available.
protonup-qt-bin
heroic-games-launcher-bin
