# SevenOS Pulse optional overlays
# These improve the Pulse experience but should not block the profile.
gamescope
mangohud
goverlay
vulkan-tools
lutris
wine
winetricks
protontricks
# Steam and 32-bit runtime packages require Arch multilib on many machines.
# SevenOS exposes them through `seven-pulse doctor` and `sevenpkg pulse install`
# so the user gets a clear guided action instead of a failing hidden install.
