#!/usr/bin/env bash
set -euo pipefail

cat <<'EOF'
SevenOS Live
African first Linux environment

Repository:
  /opt/SevenOS

Identity:
  Obsidian shell, ancestral gold, clay signal, baobab green.

Start here:
  cd /opt/SevenOS
  seven doctor
  seven status
  sevenpkg meta
  sevenosctl doctor
  sevenosctl status
  ./install.sh doctor
  ./install.sh status
  ./install.sh installer-plan
  ./install.sh base --dry-run

Build status:
  This is an early Archiso foundation, not the final installer.
EOF
